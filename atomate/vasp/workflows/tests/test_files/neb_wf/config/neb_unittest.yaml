# A testing CI-NEB calculation
# Run a fake vasp command instead of "mpirun -np $NCPUS vasp"
# Instead 'echo hello 24 world' as a Fake Vasp command

fireworks:
- fw: atomate.vasp.fireworks.core.NEBRelaxationFW
  user_incar_settings:
    ISIF: 3
    LDAU: False
- fw: atomate.vasp.fireworks.core.NEBRelaxationFW  # Two endpoints settings are the same
  user_incar_settings:
    EDIFF: 5e-5
    EDIFFG: -0.05
- fw: atomate.vasp.fireworks.core.NEBRelaxationFW
  user_incar_settings:
    EDIFF: 5e-5
    EDIFFG: -0.05
- fw: atomate.vasp.fireworks.core.NEBFW
  user_incar_settings:
    IMAGES: 3
    EDIFFG: -0.05
    IOPT: 3
    ISIF: 2
    LDAU: False
- fw: atomate.vasp.fireworks.core.NEBFW
  user_incar_settings:
    IMAGES: 3
    EDIFFG: -0.03
    IOPT: 1
    ISIF: 2
    LDAU: False

is_optimized: False

# These can update spec_orig in neb.py
common_params:
  wf_name: LiO2_test
  vasp_cmd: world
  gamma_vasp_cmd: world_gam
  mpi_command:
    command: echo
    np_tag: hello
  ppn: 24
  path_sites: [0, 1]
  db_file: ">>db_file<<"
  dest_dir: null
  _category: atomate_neb
  _queueadapter:
    nnodes: 1  # This is subject to change
